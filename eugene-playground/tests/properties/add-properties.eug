include "./tests/design-space.h"

Property sequence(txt);

/* add Property to a PartType */
P1.addProperty(sequence);
println(P1);

/* add Property to a Part */
p1_1.addProperty(sequence);
println(p1_1);

/* add Property to a Device */
Device D1(P1, P1, P1);
D1.addProperty(sequence);
println(D1);

Property prop1(txt);
Property prop2(num);

Device D2(prop1);
D2.addProperty(prop2);

D2.prop1 = "ATCG";
D2.prop2 = 5;